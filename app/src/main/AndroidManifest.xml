<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kim_dev">

    <!-- 인터넷 권한 요청 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--바코드 권한 요청 -->
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <!-- 카메라 권한 요청 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 위치 정보 엑세스 권한 요청 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- 센서 사용 권한 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <!-- 가속도계 센서 - 진동, 운동가속을 감지하는 기능 사용 (QR_code 페이지) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kim_Dev"
        tools:targetApi="31">
        <activity
            android:name=".Intro"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 메인 페이지 -->
        <activity android:name=".MainActivity"></activity>

        <!-- 로그인 페이지 -->
        <activity android:name=".Login.Login"></activity>

        <!-- 회원가입 페이지 -->
        <activity android:name=".Login.Join"></activity>

        <!-- 개발 프로젝트 페이지 -->
        <activity android:name=".Dev_Project.Dev_Project"></activity>
        <activity android:name=".Dev_Project.Dev_Project_Git"></activity>

        <!-- 프로필 페이지 -->
        <activity android:name=".Profile.Profile"></activity>

        <!-- 자격증 페이지 -->
        <activity android:name=".License.License"></activity>
        <activity android:name=".License.License_insert"></activity>
        <activity android:name=".License.License_update"></activity>
        <activity android:name=".License.License_delete"></activity>

        <!-- 시계 페이지 -->
        <activity android:name=".Clock.Clock"></activity>

        <!-- 계산기 페이지 -->
        <activity android:name=".Calculator.Calculator"></activity>

    </application>
</manifest>