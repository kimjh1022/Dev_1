<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.kim_dev">

    <!-- 인터넷 권한 요청 -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--바코드 권한 요청 -->
    <uses-sdk tools:overrideLibrary="com.google.zxing.client.android" />

    <!-- 카메라 권한 요청 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- 위치 정보 엑세스 권한 요청 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <!-- 센서 사용 권한 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>

    <!-- 가속도계 센서 - 진동, 운동가속을 감지하는 기능 사용 (QR_code 페이지) -->
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="true"/>

    <!-- 저장 기능 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kim_Dev"
        tools:targetApi="31">
        <activity
            android:name=".Intro"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

<!--                <category android:name="android.intent.category.HOME"/>-->
<!--                <category android:name="android.intent.category.DEFAULT"/>-->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 메인 페이지 -->
        <activity android:name=".a_Connection.Connection_Main"/>

        <!-- 메인 페이지 -->
        <activity android:name=".MainActivity"/>

        <!-- 로그인 페이지 -->
        <activity android:name=".Login.Login"/>

        <!-- 회원가입 페이지 -->
        <activity android:name=".Login.Join"/>

        <!-- 개발 프로젝트 페이지 -->
        <activity android:name=".Dev_Project.Dev_Project"/>
        <activity android:name=".Dev_Project.Dev_Project_insert"/>
        <activity android:name=".Dev_Project.Dev_Project_Git"/>

        <!-- 프로필 페이지 -->
        <activity android:name=".Profile.Profile"/>

        <!-- 자격증 페이지 -->
        <activity android:name=".License.License"/>
        <activity android:name=".License.License_insert"/>
        <activity android:name=".License.License_update"/>

        <!-- 시계 페이지 -->
        <activity android:name=".Clock.Clock"/>

        <!-- 계산기 페이지 -->
        <activity android:name=".Calculator.Calculator"/>

        <!-- 그림판 페이지 -->
        <activity android:name=".Drawing_Board.Drawing_board"/>

        <!-- 달력 페이지 -->
        <activity android:name=".Calendar.Calendar"/>

        <!-- 메모 페이지 -->
        <activity android:name=".Memo.Memo"/>
        <activity android:name=".Memo.Memo_insert"/>
        <activity android:name=".Memo.Memo_update"/>

        <!-- 지도 페이지 -->
        <activity android:name=".Map.Map"/>

        <!-- 날씨 페이지 -->
        <activity android:name=".Weather.Weather"/>

        <!-- 번역기 페이지 -->
        <activity android:name=".Translation.Translation"/>

        <!-- 로또 페이지 -->
        <activity android:name=".Lotto.Lotto"/>

        <!-- 칵테일 레시피 페이지 -->
        <activity android:name=".Cocktail.Cocktail"/>
        <activity android:name=".Cocktail.Cocktail_insert"/>
        <activity android:name=".Cocktail.Cocktail_update"/>

    </application>
</manifest>